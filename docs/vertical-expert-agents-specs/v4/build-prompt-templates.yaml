meta:
  command: /v4/build-prompt-templates
  agent_id: V4
  agent_title: Commercial & Facilities Expert
  version: 1
  status: draft
intent: Generate `build-prompt-templates` artifact for Commercial & Facilities Expert.
inputs:
  required:
    - user_goal
    - site_or_matter_context
  optional:
    - budget_range
    - timeline
    - constraints
processing_rules:
  - identify ambiguities before finalizing
  - return actionable and provider-ready structure
  - include missing_information_list when assumptions are used
outputs:
  primary_artifact: prompt-templates
  schema:
    summary: string
    assumptions: string[]
    missing_information_list: string[]
    recommendations: string[]
