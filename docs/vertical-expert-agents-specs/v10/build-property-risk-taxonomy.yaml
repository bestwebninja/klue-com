meta:
  command: /v10/build-property-risk-taxonomy
  agent_id: V10
  agent_title: Property Valuation & Advisory Expert
  version: 1
  status: draft
intent: Generate `build-property-risk-taxonomy` artifact for Property Valuation & Advisory Expert.
inputs:
  required:
    - user_goal
    - site_or_matter_context
  optional:
    - budget_range
    - timeline
    - constraints
processing_rules:
  - identify ambiguities before finalizing
  - return actionable and provider-ready structure
  - include missing_information_list when assumptions are used
outputs:
  primary_artifact: property-risk-taxonomy
  schema:
    summary: string
    assumptions: string[]
    missing_information_list: string[]
    recommendations: string[]
