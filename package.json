{
  "name": "kluje-enterprise-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @kluje/web",
    "dev:api": "npm run dev -w @kluje/api",
    "build": "npm run build -w @kluje/shared && npm run build -w @kluje/api && npm run build -w @kluje/web",
    "typecheck": "npm run typecheck -w @kluje/shared && npm run typecheck -w @kluje/api && npm run typecheck -w @kluje/web",
    "lint": "npm run lint -w @kluje/web"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
