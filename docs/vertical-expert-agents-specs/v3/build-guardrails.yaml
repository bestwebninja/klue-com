meta:
  command: /v3/build-guardrails
  agent_id: V3
  agent_title: Living Solutions Expert
  version: 1
  status: draft
intent: Generate `build-guardrails` artifact for Living Solutions Expert.
inputs:
  required:
    - user_goal
    - site_or_matter_context
  optional:
    - budget_range
    - timeline
    - constraints
processing_rules:
  - identify ambiguities before finalizing
  - return actionable and provider-ready structure
  - include missing_information_list when assumptions are used
outputs:
  primary_artifact: guardrails
  schema:
    summary: string
    assumptions: string[]
    missing_information_list: string[]
    recommendations: string[]
