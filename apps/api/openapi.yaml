openapi: 3.1.0
info:
  title: Kluje Enterprise API
  version: 0.1.0
servers:
  - url: http://localhost:4000/api/v1
paths:
  /health:
    get:
      summary: Liveness endpoint
      responses:
        '200':
          description: API healthy
  /auth/register:
    post:
      summary: Register a user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, minLength: 8 }
      responses:
        '201':
          description: User registered
  /auth/login:
    post:
      summary: Authenticate a user and issue JWT tokens
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, minLength: 8 }
      responses:
        '200':
          description: Authenticated
  /auth/me:
    get:
      summary: Return the authenticated user from access JWT
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Authenticated user profile
        '401':
          description: Missing, invalid, or expired token
  /campaigns:
    get:
      summary: List campaigns
      responses:
        '200':
          description: Campaign list
    post:
      summary: Create campaign
      responses:
        '201':
          description: Campaign created
  /leads:
    post:
      summary: Ingest lead
      responses:
        '201':
          description: Lead accepted
  /billing/checkout-session:
    post:
      summary: Create Stripe Checkout session
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tenantId, customerId, planTier]
              properties:
                tenantId: { type: string }
                customerId: { type: string }
                planTier:
                  type: string
                  enum: [starter, growth, enterprise]
                advertiserId: { type: string }
                successUrl: { type: string, format: uri }
                cancelUrl: { type: string, format: uri }
      responses:
        '201':
          description: Checkout session created
  /billing/subscriptions/{tenantId}:
    get:
      summary: Fetch current subscription and billing status for a tenant
      parameters:
        - in: path
          name: tenantId
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Subscription found
        '404':
          description: Tenant has no active Stripe subscription
  /billing/webhooks/stripe:
    post:
      summary: Stripe webhook endpoint
      description: |
        Validates Stripe signature and updates internal subscription status tracker.
        Uses event-id idempotency to skip duplicate deliveries.
      responses:
        '200':
          description: Event processed
        '400':
          description: Signature verification failed
  /area-risk/score:
    post:
      summary: Score regional operational area risk at a safe geography level
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [input, areaProfiles]
              properties:
                input:
                  type: object
                  properties:
                    address: { type: string }
                    lat: { type: number }
                    lng: { type: number }
                    tract: { type: string }
                    zip: { type: string }
                    county: { type: string }
                    cityCluster: { type: string }
                context:
                  type: object
                  properties:
                    clientKey: { type: string }
                areaProfiles:
                  type: object
                  description: Candidate geographic profiles used for safe geography escalation
      responses:
        '200':
          description: Area risk score or safe suppression
  /ads/placements/recommendations:
    post:
      summary: Generate AI placement recommendations for a campaign brief
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [vertical, dailyBudget, targetGeos]
              properties:
                vertical: { type: string }
                dailyBudget: { type: number }
                targetGeos:
                  type: array
                  items: { type: string }
      responses:
        '200':
          description: Ranked placements returned
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
