meta:
  command: /build-v5-agent
  agent_id: V5
  agent_title: Vertical 5 Command Center Operations Expert
  version: 2
  status: draft
intent: Build the full V5 agent specification bundle for cross-functional operations orchestration.
inputs:
  required:
    - request_text
    - organization_context
  optional:
    - portfolio_context
    - policy_context
    - historical_performance
processing_rules:
  - run shared foundation dependency checks first
  - enforce Vertical 5 instruction set routing before response generation
  - apply Kluje universal operating layer overlays for normalization and governance
outputs:
  primary_artifact: v5-agent-bundle
  includes:
    - /v5/build-intake-question-tree
    - /v5/build-command-contracts
    - /v5/build-output-json-schemas
    - /v5/build-risk-taxonomy
    - /v5/build-provider-type-taxonomy
    - /v5/build-compliance-warning-library
    - /v5/build-prompt-variants
    - /v5/build-universal-operating-layer
