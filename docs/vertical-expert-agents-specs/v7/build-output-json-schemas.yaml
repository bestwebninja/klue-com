meta:
  command: /v7/build-output-json-schemas
  agent_id: V7
  agent_title: Legal Services Expert
  version: 1
  status: draft
intent: Generate `build-output-json-schemas` artifact for Legal Services Expert.
inputs:
  required:
    - user_goal
    - site_or_matter_context
  optional:
    - budget_range
    - timeline
    - constraints
processing_rules:
  - identify ambiguities before finalizing
  - return actionable and provider-ready structure
  - include missing_information_list when assumptions are used
outputs:
  primary_artifact: output-json-schemas
  schema:
    summary: string
    assumptions: string[]
    missing_information_list: string[]
    recommendations: string[]
