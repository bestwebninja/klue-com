# Lovable Instructions

This repo is continuation-first.

Do NOT regenerate the architecture.
Do NOT create duplicate systems.
Extend only from existing structure.

Architecture rules:
- UI lives in components/
- Database logic lives in lib/supabase/
- Metrics live in lib/metrics/
- Schema lives in supabase/migrations/

Always read the repo before building.
Reuse existing patterns.
